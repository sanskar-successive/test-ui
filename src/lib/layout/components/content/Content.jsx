import React from 'react'
import { Content } from '../../../generics';

const PageContent = ({ children }) => {
    return (
        <Content>
            {children}
        </Content>
    )
}

export default PageContent