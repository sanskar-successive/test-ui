// import { Table as AntdTable, Card as AntdCard, Button as AntdButton } from "antd";

// export { AntdTable as Table, AntdCard as Card, AntdButton as Button };


import {
    Table, Space, Anchor, Layout, theme, Typography, Button,
    Cascader,
    Checkbox,
    ColorPicker,
    DatePicker,
    Form,
    Input,
    InputNumber,
    Radio,
    Rate,
    Select,
    Slider,
    Switch,
    TreeSelect,
    Upload,
    Card, Descriptions, Skeleton, Empty, Pagination, Segmented, Popconfirm, message,Spin, Result
} from "antd";
import Search from 'antd/es/input/Search';
import { EditOutlined, DeleteOutlined, EyeOutlined, PlusOutlined, UploadOutlined, LockOutlined, UserOutlined,LogoutOutlined } from '@ant-design/icons';

const { Header, Content, Sider } = Layout;
const { Title } = Typography;
const { RangePicker } = DatePicker;
const { TextArea } = Input;

export {
    Table, Space, Anchor, Layout, theme, Typography, Button,
    Cascader,
    Checkbox,
    ColorPicker,
    DatePicker,
    Form,
    Input,
    InputNumber,
    Radio,
    Rate,
    Select,
    Slider,
    Switch,
    TreeSelect,
    Upload,
    Card,
    RangePicker, TextArea, Title, Header, Content, Sider, PlusOutlined, Search, Descriptions, Skeleton, Empty, Pagination, Segmented,
    EditOutlined, DeleteOutlined, EyeOutlined, Popconfirm, message, UploadOutlined, LockOutlined, UserOutlined,LogoutOutlined, Spin, Result
}